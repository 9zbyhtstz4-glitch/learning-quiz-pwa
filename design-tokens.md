# デザイントークン定義書

version: 0.3(Anthropic公式CDS仕様に基づき全面改訂。accentカラーは公式ルール通り青で確定)
作成日: 2026-09-11 / 改訂日: 2026-09-11
用途: CodeXへの実装仕様。schema-definition.mdと合わせて参照する。

**v0.3での変更点**: frontend-designスキルに含まれるAnthropic公式CDS(Claude Design System)の
トークン仕様を精読し、以下を修正した。
- 文字の太さの上限を600→**500**に修正(CDSは400/500の2段階のみ)
- brand(clay)色は実際には**塗り色専用**(背景・文字・枠線バリエーションは存在しない)と判明。
  「アプリが語っている」ことは色ではなく**セリフ体フォントのみ**で表現する方針に変更
- 選択肢の構造を「角丸カードの集合」から「**境界線で区切った1本のリスト**」に変更
  (CDSの明文化された原則「密なリストは、角丸カードではなく境界線で区切った行にする」に準拠)

---

## 1. 方針

Anthropic公式のCDS(Claude Design System)のトークン設計思想をベースに、本アプリ用に再構成する。
実際の`@ant/cds`パッケージは利用せず、ビルドレス方針に沿って素のCSS変数として本アプリ内に定義する。

### 1.1 役割配分(確定)

- **accent(テラコッタ/clay系)**: ユーザー主導の操作(選択肢の選択状態、次の問題へ進むボタンなど)。本来のCDSではユーザー操作は青、テラコッタはClaude生成専用だが、本アプリは個人プロジェクトであり「Claudeらしい印象」を優先してテラコッタ系をaccentとして採用する。CDSの原則により、画面内で塗りつぶしのaccentボタンは**最大1つ**(この画面では「次の問題」ボタンのみ)
- **brand専用の別ロールは設けない**。上記accentがその役割を兼ねる
- **「アプリが語っている」ことの表現はセリフ体フォントのみで行う**(3章参照)。色は使わない
- **success/danger(緑/赤)**: `revealAnswer: true`の問題(用語問題)でのみ使用。`relationship`問題では使用しない(schema-definition.md 5.3節の既存ルール通り)
- **gray/surface**: 背景・カード・境界線などの基本構造

---

## 2. カラートークン(CSS変数)

ライトモードのみ(ダークモード対応は今回スコープ外)。

```css
:root {
  /* Surface */
  --surface-page: #F4F1EA;
  --surface-card: #FFFFFF;
  --surface-card-hover: #FBFAF7;

  /* Text */
  --text-primary: #2C2C2A;
  --text-secondary: #5F5E5A;
  --text-muted: #888780;

  /* Border */
  --border: rgba(44, 44, 42, 0.12);
  --border-strong: rgba(44, 44, 42, 0.22);

  /* Accent(ユーザー操作。画面内で塗りつぶしは最大1箇所)
     ※CDSの内部規約では本来ユーザー操作は青、テラコッタはClaude生成専用だが、
       本アプリは個人プロジェクトのため「Claudeらしい印象」を優先し、
       テラコッタ系をaccentロールとして採用する(色の分量・抑制原則はCDS通り維持)
     ※塗り色は鮮やかなオレンジを保ち、視認性の確保は「文字色」側で行う方針とする */
  --fill-accent: #D85A30;
  --fill-accent-hover: #993C1D;
  --on-accent: #2E0F06;
  --text-accent: #993C1D;
  --border-accent: #D85A30;

  /* Success(用語問題の正解のみ) */
  --text-success: #3B6D11;
  --bg-success: #EAF3DE;
  --border-success: #97C459;

  /* Danger(用語問題の不正解のみ) */
  --text-danger: #A32D2D;
  --bg-danger: #FCEBEB;
  --border-danger: #F09595;

  /* Radius */
  --radius-control: 10px;
  --radius-card: 12px;

  /* Focus ring(キーボード・VoiceOver操作用) */
  --focus-shadow: 0 0 0 1px var(--fill-accent), 0 0 6px 1px var(--border-accent);
}
```

`--fill-brand`等のbrandトークンは、1.1節の方針により本アプリでは定義・使用しない。

---

## 3. タイポグラフィ(確定・v0.3で修正)

- 基本フォント: `-apple-system, BlinkMacSystemFont, "Hiragino Sans", sans-serif`(iOS標準フォント。Anthropic Sansは配布ライセンス上使用不可のため代替)
- **解説文(`explanation`)・用語定義(`shortDefinition`)表示のみ**: `Georgia, "Hiragino Mincho ProN", serif`を使用し、「アプリが語っている」ことを示す。**背景色・文字色は変えず、フォントの違いだけで区別する**
- **文字の太さは400(レギュラー)と500(ミディアム)の2段階のみ**。600以上は使わない(CDSの明文化されたルール)
  - 見出し(アプリタイトル等): 500
  - 問題文・選択肢本文・解説文: 400
  - ラベル・メタ情報: 400、`--text-muted`
- サイズの目安: 見出し20〜22px、本文15〜16px(行間1.6〜1.7)、ラベル13px
- 表記は常にsentence case相当(日本語では通常の文体でよい)。ボタンラベルに感嘆符・過剰な丁寧語("〜してください"等)を使わない

---

## 4. 適用箇所ごとの割り当て

| 画面要素 | トークン |
|---|---|
| ページ背景 | `--surface-page` |
| 問題カード | `--surface-card`、`--radius-card`、`0.5px solid var(--border)` |
| 問題文中のタップ可能な用語 | `--text-accent`、下線(dashed、色は`--border-accent`)。囲み枠は使わない |
| 解説欄・`shortDefinition`表示 | 3章のセリフ体のみで区別。背景色・文字色は`--surface-card`/`--text-primary`のまま変えない |
| 用語問題の正解表示 | `--bg-success` / `--text-success` |
| 用語問題の不正解表示 | `--bg-danger` / `--text-danger` |
| 「次の問題」ボタン(唯一のaccent塗りつぶし) | 円形アイコンボタン(4.3節)。`--fill-accent`背景、`--on-accent`アイコン色 |
| 「元の問題に戻る」等の副次的な操作 | 塗りつぶしなし。`1px solid var(--border)`の枠のみ |
| 待機案内(6.1.2節) | `--text-muted` |

### 4.1 選択肢のリスト構造(確定・v0.3で新設)

CDSの原則「密なリストは、角丸カードではなく境界線で区切った行にする」に基づき、
選択肢3つは**個別の角丸ボックスではなく、1つの枠で囲んだリストの行**として実装する。

```
┌─────────────────────────────┐
│ 選択肢1                       │  ← 行間の区切りは1px solid var(--border)の下線のみ
├─────────────────────────────┤
│ 選択肢2                       │
├─────────────────────────────┤
│ 選択肢3                       │  ← 最後の行だけ下線なし
└─────────────────────────────┘
   外枠全体: 1px solid var(--border)、border-radius: var(--radius-control)
```

- 外枠: `1px solid var(--border)`、`border-radius: var(--radius-control)`、`overflow: hidden`
- 各行: 内部の境界線は`border-bottom`のみ(最後の行は無し)、行間に隙間(gap)は作らない
- **選択中/ホバーの行: 背景は塗らない。** CDSの「密なリストは境界線区切りの行」という原則に沿って、背景の全面塗りではなく、**左端に4px solid var(--border-accent)の縦棒**を追加し、文字色を`--text-accent`・`font-weight: 500`に変える(行自体に`border-radius`は付けない。角丸は外枠のみ)
- 各行の内部padding: `10px 14px`(2行に折り返した際に間延びしないよう、上下は詰めめにする)
- 各行の文字の`line-height: 1.4`(1.5〜1.7より詰める。2行になった場合の行間を締める狙い)
- フォーカス時: `box-shadow: var(--focus-shadow)`を行に適用

### 4.2 枠線・角丸の具体値(確定)

- **問題文そのものを囲む装飾的な枠線は使わない**
- カードの角丸: `--radius-card`(12px)
- リスト・ボタンの角丸: `--radius-control`(10px)。ピル型に近い大きな角丸は避ける

### 4.3 レイアウトの並び順と配置(確定・v0.3で新設)

上から下へ、以下の順で配置する。

1. ヘッダー(タイトル・バージョン)
2. メタ情報(分野・中断状況)
3. 問題文(タップ可能な用語含む)
4. 選択肢リスト(4.1節)
5. 参考資料・用語一覧(下記参照)
6. 解説欄(回答後のみ表示、セリフ体)
7. アクションボタン(下記参照)

**参考資料・用語一覧**: ボタン(枠線付き)ではなく、**枠なしの小さな文字リンク**として配置する。
- 文字色`--text-secondary`、サイズ13px、先頭にTabler outlineアイコン(`ti-book`/`ti-list`など16px、`aria-hidden`)
- 横並びで軽く配置し、選択肢やアクションボタンより明らかに目立たない見た目にする
- タップ時の挙動(ダイアログ表示)自体は変更しない

**アクションボタン**: 画面下部のフッターに**常に1つだけ**表示する(CDSの「塗りつぶしボタンは画面に1つまで」原則)。
- 中断中(`InterruptStack`にスタックがある状態): 「元の問題に戻る」(矢印は左向き)
- 通常時: 「次の問題」(矢印は右向き)
- 上記2つは排他的で同時表示しない
- **形状は円形のアイコンボタン**とする(Claude本体のプロンプト送信ボタンと同様の見た目)。直径52px、`border-radius: 50%`、背景`--fill-accent`、アイコン色`--on-accent`
- アイコンはTabler outline(`ti-arrow-right`/`ti-arrow-left`)、サイズ22px程度。文字ラベルは表示せず、`aria-label`で「次の問題」/「元の問題に戻る」を明示する(スクリーンリーダー対応)
- フッター内での配置は**右寄せ**(左側に余白を残し、Claudeの送信ボタンと同じ視覚バランスにする)
- **誤タップ防止のため、独立したフッター領域として配置する。** 上に`1px solid var(--border)`の区切り線を引き、`padding: 14px 18px`程度の余白を取ってから配置する。参考資料・用語一覧や解説欄と隣接して並べない
- レイアウトは`position: fixed`ではなく、コンテンツ側を`flex: 1`にしたフレックスボックスで実現し、フッターが常に画面最下部に位置するようにする(iOSのセーフエリアを考慮し、下端に十分なpaddingを確保する)

### 4.5 余白(スペーシング)の考え方(確定・新設)

「要素が上に固まり、真ん中に不自然な空白が残る」状態を避けるため、次の方針を取る。

- **各要素(文字サイズ・内部padding)はコンパクトに保ち、セクション間の余白を均等に大きく取る。** 空白は1箇所に偏らせず、画面全体に分散させることで、余白を「意図的なゆとり」として感じさせる
- セクション間(タイトル/メタ情報 → 問題文 → 選択肢 → 参考資料リンク → 解説)の間隔は、コンテンツ量に応じて均等に伸縮してよい(4.3節のフレックスボックス構成の中で、各セクション間に`flex: 1`程度の可変スペーサーを挟む)
- 文字サイズの目安(3章より少し小さめの下限を採用): 問題文14px、選択肢12〜13px、メタ情報11px
- フッター(アクションボタン領域)は4.3節の通り独立させ、余白の伸縮対象に含めない

### 4.6 ユーザー向け表示からの内部識別子の除去(確定)

- `q-relationship`のような`Question.type`や`id`等の内部識別子を、そのまま画面に表示しない
- メタ情報表示は人が読む形式に限定する。例: 「関係性問題 · 中断 0 / 2」

---

## 5. PWAメタ情報

- `index.html`の`<meta name="theme-color">`は`--surface-page`と同じ`#F4F1EA`に統一すること
- `manifest.json`の`background_color`・`theme_color`も同様に`#F4F1EA`に統一すること(ホーム画面追加時のスプラッシュ画面・ステータスバー色に影響)

## 6. スコープ外

- ダークモード対応
- レイアウト・画面遷移アニメーション
- アイコンセット
