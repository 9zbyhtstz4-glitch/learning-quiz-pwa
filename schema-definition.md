# 学習クイズPWA スキーマ定義書

version: 0.2
作成日: 2026-09-11 / 更新日: 2026-09-11(進捗ルール・配信先・スタック上限を確定)
用途: CodeXへの実装仕様。設計はClaudeが担当し、実装報告を受けて本書との整合性を確認・更新する。

---

## 1. 概要

Anthropic公式ドキュメント等を参考書とし、LLM・通信技術・Claudeの使い方・IT全般の用語を、
iPhoneのPWA(完全オフライン・ローカル動作)で学習するアプリのデータ構造を定義する。

### 1.1 設計の核となる方針

- 記事(`Article`)は「問われる対象」ではなく参考書。記事を分析して用語を抽出し、そこから問題を作る。
- 問題は用語起点。「単体用語問題」と「関係性問題」の2種類に統合する。
- 単体用語問題は記事ごとに派生させず、独立した用語辞書として事前に用意する。
- 優先順位は「問題全体の理解」より「用語単体の理解」が先。
- 難易度分類は存在しない。再出題制御は経過条件(問数・時間・連続正答)のみで行う。
- 三択の選択肢パターンは複数用意し、経験からの傾向推測を防ぐ。

---

## 2. エンティティ関係(概略)

```
Article(参考書) ---分析元---> Term(用語辞書)
Article(参考書) ---分析元---> Question(type: relationship)

Term ---questionIds---> Question(type: term)
Question(type: relationship) ---termIds---> Term(複数)

Progress ---questionId(1:1)---> Question
InterruptStack ---questionId(一時参照)---> Question
```

---

## 3. Article(参考書)

| 項目 | 型 | 説明 |
|---|---|---|
| `id` | string | 記事を一意に識別するID |
| `title` | string | 記事タイトル |
| `url` | string | 参考記事の実URL。ユーザーが実webで開ける参照リンク |
| `source` | string | 出典種別。例: `anthropic-docs` / `claude-support` / `lecture-video` |
| `fieldId` | string | この記事が主に該当する分野ID |

**役割の注意点**: `Article`はコンテンツ制作(用語分析・関係性分析)の元ネタ、および出典リンクの表示にのみ使う。
ユーザーが記事の内容そのものを暗記・読解しているかを問う問題は作らない。

---

## 4. Term(用語辞書)

| 項目 | 型 | 説明 |
|---|---|---|
| `id` | string | 用語を一意に識別するID。`Question.body`内の`{{term:id}}`記法から参照される |
| `label` | string | 用語の表記(画面表示用) |
| `shortDefinition` | string | 一言レベルの短い定義。用語カードの一次表示に使う |
| `fieldId` | string | この用語が属する分野ID |
| `relatedTermIds` | string[] | 関連する他の用語のID一覧(任意) |
| `questionIds` | string[] | この用語を扱う単体用語問題のID一覧。用語タップ遷移の中核項目 |
| `referenceArticleIds` | string[] | この用語の解説根拠となった記事ID(複数可。特定の1記事に紐づけない) |

**設計上の注意**: 用語は特定の記事に従属しない独立エンティティとして扱う。
複数記事に同一用語が登場しても、`Term`エントリは1つに統合する。

---

## 5. Question(問題)

| 項目 | 型 | 説明 |
|---|---|---|
| `id` | string | 問題を一意に識別するID |
| `type` | `"term"` \| `"relationship"` | 単体用語問題か関係性問題かの区分 |
| `fieldId` | string | この問題が属する分野ID |
| `patternType` | string | 選択肢パターン種別(5.2参照)。傾向推測防止のための多様化・監査用タグ |
| `body` | string | 問題文。用語部分を`{{term:用語id}}`で埋め込み、タップ可能にする |
| `choices` | string[3] | 三択の選択肢。表示時にシャッフルするが、`answerIndex`および`Progress.answerHistory.choiceIndex`は常にこの配列の固定インデックス(シャッフル前)を指す |
| `answerIndex` | number | 正解の位置(データ上の固定値。表示時は毎回シャッフルする) |
| `revealAnswer` | boolean | 回答後に正解を明示表示するかどうか(5.3参照) |
| `explanation` | string | 回答後に表示する解説文 |
| `termIds` | string[] | 問題文中に登場する用語のID一覧(タップ遷移用) |
| `sourceArticleIds` | string[] | この問題の制作根拠となった記事ID(出題内容の対象ではなく、制作トレーサビリティ用) |
| `generatedBy` | string | 選択肢の生成元。例: `gemini-3.8-flash` / `claude` / `manual`(監査用) |

### 5.1 type別の役割

- **`term`(単体用語問題)**: 用語そのものの定義・性質を問う。記事から独立して事前に用意する。
- **`relationship`(関係性問題)**: 記事分析から見つけた、複数用語間の関係性を問う。`termIds`は2つ以上を想定。

### 5.2 patternType(選択肢パターン。例)

| 値 | 内容 |
|---|---|
| `select-correct` | 正しいものを1つ選ぶ(標準形) |
| `select-incorrect` | 誤っているものを1つ選ぶ(標準形の反転) |
| `select-best` | 最も適切なものを選ぶ(複数が部分的に正しく見える) |
| `relationship` | 関係性として正しいものを選ぶ |
| `fill-blank` | 文中の空欄に入る語句を選ぶ |

新規パターン追加時は本書に追記し、CodeX側の分岐実装に反映する。

### 5.3 revealAnswerの方針(確定)

- `type: "term"` の問題 → `revealAnswer: true`(回答後に正解を明示。土台となる用語は速く確定させる)
- `type: "relationship"` の問題 → `revealAnswer: false`(正解を明示せず、`explanation`のみ表示して理解に導く。概念解説のみ方式で確定)

---

## 6. Progress(ユーザー学習進捗・端末内保存)

| 項目 | 型 | 説明 |
|---|---|---|
| `questionId` | string | 対象の問題ID |
| `state` | `"unseen"` \| `"cooling"` \| `"due"` \| `"resting"` | 出題状態 |
| `lastSeenAt` | timestamp \| null | 最後に出題した日時。ミリ秒のUnix時刻。未出題は`null` |
| `seenSinceCount` | number | 出題後、他に何問経過したか |
| `correctStreak` | number | 連続正答数 |
| `answerHistory` | { choiceIndex, correct, timestamp }[] | 回答履歴。理解度グラフの集計元 |

### 6.1 状態遷移(確定)

```
unseen  → cooling(出題直後)
cooling → due     (条件: 他の問題をN問経過。設定ファイルで変更可能。暫定値: 30問。経過時間の条件は2026-09-13に廃止)
due     → resting (条件: 連続正答2回)
due     内での誤答 → 状態はdueのまま、correctStreakを0にリセット
resting → due     (条件A: 最後の出題からD日経過。設定ファイルで変更可能。暫定値: 7日)
resting → due     (条件B: resting状態で誤答した場合は即座に降格)
```

出題優先度: `unseen` > `due` > `resting`

**注記**: これは難易度分類ではなく、経過条件のみによる制御。

#### 6.1.1「他の問題を経過」のカウント方法(確定)

- 用語タップによる遷移先の問題(`type: "term"`)も1問としてカウントする
- 中断元への復帰(`InterruptStack`のpop)はカウントしない
- 中断中も、選択肢の表示順・回答状態は保持する(中断元に戻った際に再現する)

#### 6.1.2 出題可能な問題が存在しない場合の挙動(確定)

出題可能な問題(`unseen` / `due` / `resting`)が存在せず、全問題が`cooling`の場合の扱いは以下の通り。

- 停止するのは「次の問題」の**自動選択のみ**。学習動線そのものは止めない
- 用語タップによる`InterruptStack`遷移は、遷移先が`cooling`状態でも継続して利用可能とする
- `InterruptStack`の深さが上限に達した場合も、7.1節の通り該当用語の`shortDefinition`を表示して確認できるようにする
- 待機中も直前の問題文と用語タップの導線(タップ可能な用語表示)を維持したまま、待機案内を表示する
- 用語タップ経由でアクセスした`term`問題への回答は、通常通り`Progress`・`answerHistory`に保存する

---

## 7. InterruptStack(中断スタック。セッション内の一時状態)

用語タップによる問題遷移を管理するLIFOスタック。

| 項目 | 型 | 説明 |
|---|---|---|
| `stack` | { questionId }[] | 中断中の問題IDを積んだ配列 |
| `maxDepth` | number | スタックの上限(確定値: 2) |

**永続化範囲(確定)**: `InterruptStack`はセッション内の一時状態であり、ページ再読み込みをまたいだ復元は行わない。選択肢の表示順・回答状態・待機状態も同様にセッション内でのみ保持する。再読み込み時に出題可能な問題がなく全問`cooling`の場合は、6.1.2節の閲覧用表示(直前の問題文・用語一覧)を示し、これは新規出題としてカウントしない。

### 7.1 動作フロー(確定)

1. 問題文中の用語をタップ → **循環判定(7.2節)を行う** → 循環に該当しなければ、現在の`questionId`を`stack`にpush → 遷移先の用語問題(`type: term`)を表示。遷移先が`cooling`状態であっても表示は妨げない
2. 遷移先の問題に回答 → `revealAnswer: true`のため正解を表示。回答は通常通り`Progress`・`answerHistory`に保存する
3. ユーザー操作(戻るボタン)で`stack`をpop → 中断していた問題を再表示して続行(選択肢順・回答状態を保持)
4. `stack.length >= maxDepth`の場合、新規遷移(問題への遷移)を行わず、該当用語の`shortDefinition`のみを表示するフォールバックとする

### 7.2 循環参照の防止(確定)

用語タップの遷移先を解決した結果、以下のいずれかに該当する場合は新規遷移を行わず、7.1節4と同じフォールバック(該当用語の`shortDefinition`のみを表示)とする。

- 遷移先の問題が、**現在表示中の問題と同一**の場合(自己参照。例: `API`の用語問題本文中に`{{term:api}}`が含まれるケース)
- 遷移先の問題が、**`stack`内に既に存在する**問題と同一の場合(循環。例: A→B→Aのような参照)

タップ可能な見た目(用語のハイライト表示)自体は変更しない。遷移の可否のみをこの判定で制御する。

### 7.3 自己参照マーカーの禁止(確定・データ作成時の制約)

**`type: "term"`の問題の`body`には、その問題自身が扱う用語の`{{term:id}}`マーカーを含めてはならない。**

理由: 7.2節のフォールバックにより、自己参照をタップすると該当用語の`shortDefinition`が表示される。
用語問題においてこれは**答えそのものの露出**にあたり、学習効果を損なう。

- `type: "relationship"`の問題では、この制約は適用されない(複数用語の関係性を問うため、
  個々の用語の定義が見えても答えの露出にはならない)
- データ投入時の検証スクリプトで、この制約を機械的にチェックすること

---

## 8. 未確定事項(本書の次回更新で確定させる項目)

1. 理解度(円グラフ)の算出式 — 正答率か、用語問題の充足度を主軸にするか
2. 選択肢パターン(`patternType`)の傾向監査の実施方法・頻度の具体的な運用手順

### 8.1 確定済み事項(参考。旧版からの変更点)

- `relationship`問題の答えの見せ方 → 概念解説のみ方式で確定(5.3節)
- 配信先 → GitHub Pages(1GB容量・100GB/月帯域の無料枠で本アプリの規模には十分)
- `InterruptStack.maxDepth` → 2で確定
- `cooling → due` / `resting → due`の具体的な条件・カウント方法 → 6.1節参照
- `cooling → due`の経過時間の条件(24h)を廃止し、他問数の条件のみとした(2026-09-13)。全問を1日で出題し終えると、24時間待つまで閲覧専用の画面から進めなくなっていたため

---

## 9. 実装状況

- 骨組み実装完了(CodeX)。三択シャッフル/回答分岐、用語タップと中断スタック、Progress永続化(IndexedDB)、全問`cooling`時の用語アクセス継続、オフラインキャッシュ、ダミーデータ(問題6・用語5・記事5)、閾値変更設定を実装済み
- 自動テスト9件・IndexedDB検証3件成功。中断/復帰/待機時操作、静的サーバー停止後の回答・再読み込みを手動確認済み
- GitHub Pagesへの公開完了。公開URL: https://9zbyhtstz4-glitch.github.io/learning-quiz-pwa/ ／ リポジトリ: https://github.com/9zbyhtstz4-glitch/learning-quiz-pwa (`main`ブランチ・ルート配信、全19ファイル約48KB、独自ビルド不要)
- 未実施: **iPhone実機での機内モード確認**(ホーム画面追加後、Wi-Fiオフ+機内モードでの再起動・回答・用語遷移)。手順はリポジトリREADMEに記載済み。これはユーザー側での実機操作が必要なため、CodeXへの委任範囲外

## 10. 制作フロー上の注意(CodeX実装とは別軸だが仕様理解のため記載)

- 三択の選択肢文言はGemini 3.8 Flashが生成する。ClaudeはGeminiに対し、1問分の解説と答えのみを渡す。
- 実装(本書に基づくコード化)はCodeXが担当。Claudeは本書の作成・更新と、CodeXからの実装報告の整合性確認を行う。
- データ量増加に伴い、`patternType`分布・`answerIndex`分布・`generatedBy`ごとの文体傾向を定期監査する(新規問題50問追加ごとを目安とする)。
