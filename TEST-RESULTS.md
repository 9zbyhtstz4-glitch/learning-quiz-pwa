# 動作確認結果

実施日: 2026-09-11。Windows上のCodex内ブラウザーで確認。

## 自動テスト

- `node --test tests/progress.test.mjs`: 9件成功。
- `/tests/storage.html`: IndexedDBの再接続後の永続化、並行更新、例外時ロールバックの3件成功。テスト用DBは実行ごとに作成・削除。
- JavaScriptの構文チェック成功。

## ブラウザー操作

- `/quiz/`サブパスでES Modules、JSON、CSS、manifest、Service Workerを使用して起動。
- 初回キャッシュ完了の表示を確認。
- 関係性問題に回答し、正誤・正解を表示せず概念解説のみを表示。
- 関係性問題→PWA→APIへ遷移し、深さ2でHTTPをタップすると短い定義だけを表示。
- APIの回答後に正解表示。PWAへ戻り、未回答状態と選択肢順が保存されていることを比較確認。
- PWA回答後に関係性問題へ戻り、回答済み状態・解説・選択肢順が保存されていることを比較確認。
- 残りの通常出題を完了し、全問cooling時に直前の問題と用語リンクを保持した待機画面を確認。
- 静的サーバーを停止し、待機画面からcooling中のPWA問題へ遷移・回答・復帰できることを確認。
- サーバー停止中にページを再読み込みし、キャッシュから起動して待機状態になることを確認。
- サーバー再開後、読み取り専用の進捗表示でオフライン回答履歴を確認。
- q-relationshipのseenSinceCountは6（PWA、API、HTTP、キャッシュ、IndexedDB、再度PWA）。スタック復帰と上限時のHTTP定義表示は加算されていない。

## 未検証

- iPhone Safari実機のホーム画面追加、ホーム画面からの起動、Wi-Fiオフ／機内モード。
- iPhone実機でのGitHub Pages公開URLへのアクセス。
- 24時間／7日の実時間待機。境界値は時刻を指定する自動テストで確認。

ブラウザーのサーバー停止テストは配信元に接続できない条件での検証であり、iPhone実機のオフライン受入試験とは区別する。

## GitHub Pages公開後の確認

- Publicリポジトリ `9zbyhtstz4-glitch/learning-quiz-pwa` を作成しpush。
- mainブランチのルート（/）から公開。HTTPS強制有効。独自のビルド／Actions設定は不要。
- https://9zbyhtstz4-glitch.github.io/learning-quiz-pwa/ で起動・用語遷移・回答・復帰を確認。
- `/tests/pwa.html` の検証成功。Service Workerが公開サブパスでactive、必要資源12/12件がキャッシュ済み。
- manifestのstandalone、scope、start_url、192/512 PNGアイコンを確認。Apple touch iconもキャッシュ済み。
- 公開URLでネットワークを切断して再起動する試験は未実施（使用ブラウザーに切断操作がない）。ローカル配信元停止テストの成功と区別する。
- iPhone実機は利用できず、Safariのホーム画面追加操作は未検証。READMEの機内モード・Wi-Fiオフでの確認手順を使用する。

## デザイントークン適用

2026-09-11: design-tokens.md v0.1のCSS変数をそのままstyle.cssに定義。操作は青、解説・定義だけテラコッタとセリフ体、用語問題の結果だけ緑・赤を使用。レイアウトとダミーデータは維持。
確認方法: 用語問題で回答し、結果はサンセリフ、解説はセリフであることを確認。同じ用語をタップすると同じテラコッタの定義欄を表示。関係性問題では正誤色を表示しない。
自己参照・A→B→A・上限・通常遷移と既存進捗のテストは計13件成功。ブラウザーで解説・定義・結果・ボタンの実際のフォントと色を確認済み。
